name: event test
on: push

jobs:
  build:
    name: build action
    runs-on: ubuntu-latest    
    steps:
    - run: |
        echo "$GITHUB_ACTION"
        echo "$GITHUB_EVENT_NAME"
        echo "$GITHUB_EVENT_PATH"
        cat "$GITHUB_EVENT_PATH"


